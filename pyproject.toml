[project]
name = "versal-mosse"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy",
]

[project.optional-dependencies]
weights = [
    "torch",
    "torchvision",
]
vot = [
    "vot-toolkit",
]
